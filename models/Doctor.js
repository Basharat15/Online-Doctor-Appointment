export default class Doctor {
       constructor(id, ownerId, title, category, address, descryption, phonenum, timing, imgUrl, holiday,date ){
              this.id= id;
              this.ownerId= ownerId;
              this.title= title;
              this.category= category;
              this.address= address;
              this.descryption= descryption;
              this.timing= timing;
              this.phonenum= phonenum;
              this.imgUrl= imgUrl;
              this.holiday= holiday;
              this.date= date;
              
       };
};
